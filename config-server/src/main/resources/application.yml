server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/erik-ggg/micros-config-repo
          clone-on-start: true
          default-label: main

